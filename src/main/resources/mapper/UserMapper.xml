<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.anime.mapper.UserMapper">

    <select id="findByUsername" parameterType="string" resultType="com.anime.domain.User">
        SELECT id, username, password_hash AS passwordHash,
               nickname, email, role, status,
               created_at AS createdAt, updated_at AS updatedAt,
               last_login_at AS lastLoginAt
        FROM users WHERE username = #{username} LIMIT 1
    </select>

    <insert id="insertUser" parameterType="com.anime.domain.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, password_hash, nickname, email, role, status)
        VALUES (#{username}, #{passwordHash}, #{nickname}, #{email}, #{role}, #{status})
    </insert>

    <select id="existsByUsername" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM users WHERE username = #{username}
    </select>

    <select id="findById" resultType="com.anime.domain.User">
        SELECT id, username, password_hash AS passwordHash,
               nickname, email, role, status,
               created_at AS createdAt, updated_at AS updatedAt,
               last_login_at AS lastLoginAt
        FROM users WHERE id = #{id}
    </select>

    <select id="findIdByUsername" resultType="long">
        SELECT id FROM users WHERE username = #{username}
    </select>

    <update id="updateLastLogin" parameterType="string">
        UPDATE users SET last_login_at = CURRENT_TIMESTAMP(3) WHERE username = #{username}
    </update>

    <!-- 닉네임/이메일 수정 -->
    <update id="updateProfile" parameterType="com.anime.domain.User">
        UPDATE users
        SET nickname = #{nickname},
            email    = #{email}
        WHERE id = #{id}
    </update>

    <!-- 비밀번호 변경 -->
    <update id="updatePassword" parameterType="com.anime.domain.User">
        UPDATE users
        SET password_hash = #{passwordHash}
        WHERE id = #{id}
    </update>

    <!-- 회원 탈퇴 (완전 삭제) -->
    <delete id="deleteUser">
        DELETE FROM users WHERE id = #{id}
    </delete>

    <!-- 관리자: 전체 회원 목록 -->
    <select id="findAll" resultType="com.anime.domain.User">
        SELECT id, username, nickname, email, role, status,
               created_at AS createdAt, last_login_at AS lastLoginAt
        FROM users
        ORDER BY created_at DESC
    </select>

    <!-- 관리자: 강제 탈퇴 -->
    <delete id="deleteUserById">
        DELETE FROM users WHERE id = #{id}
    </delete>

</mapper>